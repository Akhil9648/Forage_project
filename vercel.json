{
  "version": 2,
  "builds": [
    {
      "src": "kanban-backend/server.js",
      "use": "@vercel/node"
    },
    {
      "src": "kanban-frontend/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      }
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "kanban-backend/server.js"
    },
    {
      "src": "/(.*)",
      "dest": "kanban-frontend/$1"
    }
  ]
}
